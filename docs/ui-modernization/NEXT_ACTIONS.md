# Next Actions

1. Start Phase 2 in `/Users/rubenvancraenenbroeck/IdeaProjects/LDLib-MultiLoader`.
2. Keep the first code pass conservative and visual-only:
   - add richer shared editor color/panel constants,
   - give ConfigPanel and its scroll regions a real background/card surface,
   - strengthen menu/dialog/float-view shared surfaces,
   - improve top/left/bottom shell contrast without changing editor behavior.
3. Compile LDLib after the Phase 2 pass.
4. Review diffs for behavior changes and fix any accidental runtime logic changes.
5. Commit the LDLib Phase 2 checkpoint, then update this Photon tracker with the commit hash and validation results.
6. Continue into Phase 3 configurator usability after Phase 2 is clean.
